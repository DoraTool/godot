name: 🌐 Web Editor Deploy to Cloudflare R2
on:
  push:
    branches:
      - '4.5'
    tags:
      - '*'

# Global Settings
env:
  SCONS_FLAGS: >-
    dev_mode=yes
    tests=no
    debug_symbols=no
    use_closure_compiler=yes
  EM_VERSION: 4.0.11
  CLOUDFLARE_PROJECT_NAME: gambo-engine
  CLOUDFLARE_R2_BUCKET: gambo-engine

jobs:
  build-and-deploy:
    runs-on: ubuntu-24.04
    name: Build and Deploy Web Editor
    timeout-minutes: 60
    permissions:
      contents: read
      deployments: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Set up Emscripten latest
        uses: mymindstorm/setup-emsdk@v14
        with:
          version: ${{ env.EM_VERSION }}
          no-cache: true

      - name: Verify Emscripten setup
        run: |
          emcc -v

      - name: Restore Godot build cache
        uses: ./.github/actions/godot-cache-restore
        with:
          cache-name: web-editor
        continue-on-error: true

      - name: Setup Python and SCons
        uses: ./.github/actions/godot-deps

      - name: Compilation
        uses: ./.github/actions/godot-build
        with:
          scons-flags: ${{ env.SCONS_FLAGS }} threads=yes
          platform: web
          target: editor

      - name: Save Godot build cache
        uses: ./.github/actions/godot-cache-save
        with:
          cache-name: web-editor
        continue-on-error: true

      - name: Verify build output
        run: |
          ls -la bin/.web_zip/
          echo "Build output verified"

      - name: Determine deploy path
        id: deploy-path
        run: |
          if [[ "$GITHUB_REF" == refs/tags/* ]]; then
            # Extract tag name (remove refs/tags/ prefix)
            TAG_NAME="${GITHUB_REF#refs/tags/}"
            echo "path=$TAG_NAME" >> $GITHUB_OUTPUT
            echo "Using tag as deploy path: $TAG_NAME"
          else
            # Use commit SHA (short version)
            COMMIT_SHA="${GITHUB_SHA:0:7}"
            echo "path=$COMMIT_SHA" >> $GITHUB_OUTPUT
            echo "Using commit SHA as deploy path: $COMMIT_SHA"
          fi

      - name: Upload to Cloudflare R2
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          command: |
            cd bin/.web_zip
            DEPLOY_PATH="${{ steps.deploy-path.outputs.path }}"
            find . -type f | while read -r file; do
              # Remove leading ./ from path
              clean_path="${file#./}"
              # Upload to R2 with deploy path prefix
              wrangler r2 object put "${{ env.CLOUDFLARE_R2_BUCKET }}/${DEPLOY_PATH}/${clean_path}" --file="$file"
            done

